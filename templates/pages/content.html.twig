{% extends 'base.html.twig' %}

{% block title %}Seepferdchen‑Garde — {{ slug|default('Seite')|title }}{% endblock %}

{% block body %}
  <main id="content" class="py-5">
    <div class="container">
      <article class="markdown-body">
        {{ content|raw }}
      </article>
    </div>
  </main>
{% endblock %}
