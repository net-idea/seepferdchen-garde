# Stateless CSRF protection for forms (double-submit cookie, see assets/scripts/csrf-protection.js)
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        check_header: true
        stateless_token_ids:
            - submit
